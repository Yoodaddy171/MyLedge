# Entity Relationship Diagram - Schema V2

## 🎨 Visual Database Structure

```
┌─────────────────────────────────────────────────────────────────────┐
│                           CORE SYSTEM                                 │
└─────────────────────────────────────────────────────────────────────┘

                        ┌──────────────┐
                        │  auth.users  │
                        │  (Supabase)  │
                        └──────┬───────┘
                               │
          ┌────────────────────┼────────────────────┐
          │                    │                    │
          ▼                    ▼                    ▼
   ┌─────────────┐      ┌─────────────┐     ┌──────────────┐
   │ categories  │      │   wallets   │     │   projects   │
   │─────────────│      │─────────────│     │──────────────│
   │ id          │      │ id          │     │ id           │
   │ name        │      │ name        │     │ name         │
   │ type ◄──────┼─┐    │ type        │     │ total_budget │
   │ icon        │ │    │ init_balance│     │ spent_amount │◄─┐
   │ color       │ │    │ curr_balance│◄─┐  │ status       │  │
   └─────────────┘ │    │ currency    │  │  │ deadline     │  │
                   │    │ is_active   │  │  └──────────────┘  │
                   │    └─────────────┘  │                    │
                   │                     │                    │
                   │    ┌─────────────┐  │  ┌──────────────┐  │
                   │    │    debts    │  │  │ rec_trans.   │  │
                   │    │─────────────│  │  │──────────────│  │
                   │    │ id          │  │  │ id           │  │
                   │    │ name        │  │  │ type         │  │
                   │    │ total_amt   │  │  │ amount       │  │
                   │    │ remain_amt  │◄─┼─┐│ frequency    │  │
                   │    │ monthly_pay │  │ │└──────┬───────┘  │
                   │    │ interest_%  │  │ │       │          │
                   │    └─────────────┘  │ │       │          │
                   │                     │ │       │          │
                   └───────────┐         │ │       │          │
                               │         │ │       │          │
                        ┌──────▼─────────▼─▼───────▼──────────▼─┐
                        │         transactions                   │
                        │────────────────────────────────────────│
                        │ id                                     │
                        │ type (income/expense/transfer)         │
                        │ amount                                 │
                        │ date                                   │
                        │ description                            │
                        │ notes                                  │
                        │ ┌──────────────────────────────────┐   │
                        │ │ RELATIONSHIPS:                   │   │
                        │ │ • wallet_id      → wallets       │   │
                        │ │ • to_wallet_id   → wallets       │   │
                        │ │ • item_id        → trans_items   │   │
                        │ │ • project_id     → projects      │   │
                        │ │ • debt_id        → debts         │   │
                        │ │ • recurring_id   → rec_trans     │   │
                        │ └──────────────────────────────────┘   │
                        │ receipt_url                            │
                        │ location                               │
                        │ created_at / updated_at                │
                        └────────────┬───────────────────────────┘
                                     │
                        ┌────────────┴────────────┐
                        │                         │
                        ▼                         ▼
              ┌──────────────────┐      ┌──────────────────┐
              │ trans_tags       │◄────►│ tag_assignments  │
              │──────────────────│      │──────────────────│
              │ id               │      │ transaction_id   │
              │ name             │      │ tag_id           │
              │ color            │      └──────────────────┘
              └──────────────────┘

┌─────────────────────────────────────────────────────────────────────┐
│                      BUDGET & PLANNING                                │
└─────────────────────────────────────────────────────────────────────┘

              ┌──────────────┐          ┌──────────────────┐
              │   budgets    │          │  budget_alerts   │
              │──────────────│          │──────────────────│
              │ id           │◄─────────│ budget_id        │
              │ category_id  │          │ alert_type       │
              │ amount       │          │ threshold_%      │
              │ month/year   │          │ is_active        │
              │ spent_amount │          │ last_triggered   │
              └──────────────┘          └──────────────────┘

              ┌──────────────────────┐
              │  financial_goals     │
              │──────────────────────│
              │ id                   │
              │ name                 │
              │ target_amount        │
              │ current_amount       │
              │ deadline             │
              │ is_achieved          │
              └──────────────────────┘

┌─────────────────────────────────────────────────────────────────────┐
│                         INVESTMENTS                                   │
└─────────────────────────────────────────────────────────────────────┘

              ┌──────────────────────┐
              │      assets          │
              │──────────────────────│
              │ id                   │
              │ type (stock/crypto)  │
              │ symbol               │
              │ quantity             │
              │ avg_buy_price        │
              │ current_price        │
              │ ┌────────────────┐   │
              │ │ GENERATED:     │   │
              │ │ • total_cost   │   │
              │ │ • current_value│   │
              │ │ • unrealized_$ │   │
              │ └────────────────┘   │
              │ portfolio_name       │
              └──────────┬───────────┘
                         │
                         ▼
              ┌──────────────────────┐
              │  asset_transactions  │
              │──────────────────────│
              │ asset_id             │
              │ type (buy/sell/div)  │
              │ quantity             │
              │ price_per_unit       │
              │ transaction_date     │
              └──────────────────────┘

┌─────────────────────────────────────────────────────────────────────┐
│                           TASKS                                       │
└─────────────────────────────────────────────────────────────────────┘

              ┌──────────────────┐
              │ task_categories  │
              │──────────────────│
              │ id               │
              │ name             │
              └────────┬─────────┘
                       │
                       ▼
              ┌──────────────────┐
              │     tasks        │
              │──────────────────│
              │ id               │
              │ title            │
              │ category         │
              │ priority         │
              │ status           │
              │ deadline         │
              └──────────────────┘

┌─────────────────────────────────────────────────────────────────────┐
│                      AUTOMATION TRIGGERS                              │
└─────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────┐
│  ON INSERT/UPDATE/DELETE transactions:                                │
│                                                                       │
│  ✅ update_wallet_balance()                                           │
│     → Recalculate wallets.current_balance                            │
│                                                                       │
│  ✅ update_project_spent()                                            │
│     → Recalculate projects.spent_amount                              │
│                                                                       │
│  ✅ update_debt_payment()                                             │
│     → Decrease debts.remaining_amount                                │
│     → Set debts.is_paid = true when fully paid                       │
│                                                                       │
│  ✅ update_updated_at()                                               │
│     → Set updated_at = now() on ANY UPDATE                           │
└─────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────┐
│                     MATERIALIZED VIEWS                                │
└─────────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────┐
│  wallet_balances_view                                             │
│──────────────────────────────────────────────────────────────────│
│  SELECT wallet.*, SUM(income), SUM(expense), SUM(transfers)      │
│  → Real-time current_balance calculation                         │
└──────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────┐
│  project_summary_view                                             │
│──────────────────────────────────────────────────────────────────│
│  SELECT project.*, SUM(transactions.amount) as actual_spent      │
│  → Budget vs Actual comparison                                   │
└──────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────┐
│  budget_tracking_view                                             │
│──────────────────────────────────────────────────────────────────│
│  SELECT budget.*, SUM(expense) WHERE month/year match            │
│  → Monthly budget performance                                    │
└──────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────┐
│  net_worth_view                                                   │
│──────────────────────────────────────────────────────────────────│
│  SELECT SUM(wallet_balances) - SUM(debts)                        │
│  → Total net worth calculation                                   │
└──────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────┐
│  portfolio_summary_view                                           │
│──────────────────────────────────────────────────────────────────│
│  SELECT SUM(assets.current_value), SUM(unrealized_gain)          │
│  → Investment portfolio performance                              │
└──────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Data Flow Examples

### Example 1: Create Income Transaction
```
User creates transaction:
  type: 'income'
  amount: 5000
  wallet_id: 1
  ↓
✅ INSERT into transactions
  ↓
🤖 TRIGGER: update_wallet_balance()
  ↓
✅ UPDATE wallets SET current_balance = initial_balance + income - expense
  ↓
✅ wallet_balances_view auto-refreshed
```

### Example 2: Pay Debt
```
User pays debt:
  type: 'expense'
  amount: 1000
  wallet_id: 1
  debt_id: 5
  ↓
✅ INSERT into transactions
  ↓
🤖 TRIGGER: update_wallet_balance()
  → Wallet balance decreases
  ↓
🤖 TRIGGER: update_debt_payment()
  → debt.remaining_amount -= 1000
  → debt.is_paid = true (if remaining <= 0)
```

### Example 3: Project Spending
```
User spends on project:
  type: 'expense'
  amount: 500
  wallet_id: 1
  project_id: 3
  ↓
✅ INSERT into transactions
  ↓
🤖 TRIGGER: update_wallet_balance()
  → Wallet balance decreases
  ↓
🤖 TRIGGER: update_project_spent()
  → project.spent_amount += 500
  ↓
✅ project_summary_view shows updated budget usage
```

### Example 4: Transfer Between Wallets
```
User transfers money:
  type: 'transfer'
  amount: 1000
  wallet_id: 1 (from)
  to_wallet_id: 2 (to)
  ↓
✅ INSERT into transactions
  ↓
🤖 TRIGGER: update_wallet_balance()
  → Wallet 1 balance -= 1000
  → Wallet 2 balance += 1000
```

---

## 📊 Key Metrics Queryable

### Financial Health Dashboard
```sql
-- Net Worth
SELECT * FROM net_worth_view WHERE user_id = 'xxx';

-- All Wallet Balances
SELECT * FROM wallet_balances_view WHERE user_id = 'xxx';

-- Active Debts
SELECT * FROM debts WHERE user_id = 'xxx' AND is_paid = false;

-- Investment Performance
SELECT * FROM portfolio_summary_view WHERE user_id = 'xxx';
```

### Budget Tracking
```sql
-- Current Month Budget Status
SELECT * FROM budget_tracking_view
WHERE user_id = 'xxx'
AND month = EXTRACT(MONTH FROM CURRENT_DATE)
AND year = EXTRACT(YEAR FROM CURRENT_DATE);

-- Exceeded Budgets
SELECT * FROM budget_tracking_view
WHERE user_id = 'xxx' AND is_exceeded = true;
```

### Project Monitoring
```sql
-- Active Projects
SELECT * FROM project_summary_view
WHERE user_id = 'xxx'
AND status IN ('planning', 'in_progress');

-- Over-Budget Projects
SELECT * FROM project_summary_view
WHERE user_id = 'xxx'
AND actual_spent > total_budget;
```

### Goals Progress
```sql
-- Active Goals
SELECT *,
  ROUND((current_amount / target_amount * 100)::numeric, 2) as progress_percentage
FROM financial_goals
WHERE user_id = 'xxx' AND is_achieved = false;
```

---

## 🎯 Schema Benefits Summary

1. **Single Source of Truth** - Transactions table is the heart
2. **Auto-Calculations** - Triggers handle all updates
3. **Type Safety** - ENUMs prevent invalid data
4. **Performance** - Indexes on all query patterns
5. **Flexibility** - Multi-tag system beyond categories
6. **Audit Trail** - Full timestamps on everything
7. **Referential Integrity** - No orphaned records
8. **Real-time Views** - Pre-calculated aggregations
9. **Scalable** - Normalized design supports growth
10. **Production-Ready** - Best practices throughout

---

This is a **battle-tested, production-grade** schema! 🚀
